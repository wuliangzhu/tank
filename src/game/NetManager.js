/**
* name 
*/
var game;
(function (game) {
    var NetManager = (function () {
        function NetManager() {
        }
        return NetManager;
    }());
    game.NetManager = NetManager;
})(game || (game = {}));