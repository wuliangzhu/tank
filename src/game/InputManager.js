/**
* name 
*/
var game;
(function (game) {
    var InputManager = (function () {
        function InputManager() {
        }
        return InputManager;
    }());
    game.InputManager = InputManager;
})(game || (game = {}));