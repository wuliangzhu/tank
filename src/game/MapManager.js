/**
* name 
*/
var game;
(function (game) {
    var MapManager = (function () {
        function MapManager() {
        }
        return MapManager;
    }());
    game.MapManager = MapManager;
})(game || (game = {}));